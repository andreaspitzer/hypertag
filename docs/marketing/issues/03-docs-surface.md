# Docs surface for the VoC framing

Type: grilling
Status: open
Blocked by: 01

## Question

Given the VoC-framed README (ticket 01), decide whether the in-repo docs need aligning:

- Does `docs/api.md` (and/or `CONTEXT.md`) need to reflect the extractor-forward story and the
  README's cross-links, or is the current reference fine as-is?
- **Audit** `docs/api.md` against the seven shipped layers (core, `select`, `sanitize`, `ld`,
  `meta`, `fetch`, `oembed`) – flag anything stale or missing.

Output: the docs-structure decision + a concrete list of any `docs/api.md` corrections needed.
Blocked by 01 (docs follow the README's final shape).

## Also in scope (folded in from a README review pass, 2026-09-09)

A README audit found the per-layer **size table** (the "you import / ships (gzipped)" table) is not
reproducible from `benchmark/` – it was generated by an uncommitted scratchpad script, which let its
numbers drift (C1 provenance gap). Two figures round the wrong way and one was self-contradictory:

- **F1 (fixed in README already):** `hypertag/meta` read 4.9 kB in the layer table but 5.0 kB in the
  edge table – same module, one screen apart. Measured 4.99 kB → corrected the layer table to 5.0 kB.
- **F2 (still open):** re-measured (esbuild `--bundle --minify`, esm, gzip, node builtins external):
  `hypertag` = 6.52 kB (table says 6.6 → should be 6.5) and `hypertag/select` = 1.98 kB (table says
  1.9 → should be 2.0). Other rows (`parse` 0.7, `ld` 0.9, `sanitize` 1.4, `oembed` 1.4, `fetch` 5.1)
  re-measure correctly.
- **F3 (the fix):** add a committed size benchmark (e.g. `benchmark/layer-sizes.mjs` + a `bench:size`
  script) that emits this exact eight-row table with one methodology, then regenerate all eight README
  rows from its output in a single pass – so C1 holds for the size table the way it already does for
  the speed/memory tables. Resolve F2 by that regeneration, not by hand-patching individual cells.
